@page "/oddeven"
@inject ShowMeTheBet.ViewModels.Game.OddEvenViewModel ViewModel
@inject NavigationManager Navigation
@rendermode InteractiveServer
@implements IDisposable

<PageTitle>홀짝 게임 - ShowMeTheBet</PageTitle>

@if (ViewModel.IsLoading)
{
    <div style="text-align: center; padding: 50px;">
        <p>로딩 중...</p>
    </div>
}
else if (!ViewModel.IsAuthenticated)
{
    <div class="auth-required">
        <div class="auth-message">
            <h2>로그인이 필요합니다</h2>
            <p>게임을 하려면 먼저 로그인해주세요.</p>
            <button class="btn btn-primary" @onclick="NavigateToLogin">로그인하기</button>
        </div>
    </div>
}
else
{
    <div class="game-container">
        <div class="game-header">
            <h1>🎲 홀짝 게임</h1>
            <div class="balance-display">
                <span class="balance-label">잔액:</span>
                <span class="balance-amount">@ViewModel.Balance.ToString("N0") 원</span>
            </div>
        </div>

        <div class="game-content">
            <div class="game-main">
                <div class="round-info">
                    <h2>다음 라운드까지</h2>
                    <div class="countdown">@ViewModel.TimeLeft 초</div>
                    <div class="current-result">
                        <span class="result-label">현재 결과:</span>
                        <span class="result-value @(ViewModel.CurrentResult == "홀" ? "odd" : "even")">@ViewModel.CurrentResult</span>
                    </div>
                </div>

                <div class="bet-section">
                    <h3>베팅하기</h3>
                    <div class="bet-amount-input">
                        <label>베팅 금액:</label>
                        <input type="number"
                               class="form-control"
                               value="@ViewModel.BetAmount"
                               @oninput="OnBetAmountInput"
                               min="1000"
                               step="1000"
                               placeholder="금액 입력" />
                        <div class="quick-amount-buttons">
                            <button type="button" class="btn btn-sm btn-outline-secondary" @onclick="async () => await OnQuickBet(0.25m)" disabled="@(ViewModel.Balance <= 0 || ViewModel.TimeLeft < 5 || ViewModel.IsLoading)">
                                25%
                            </button>
                            <button type="button" class="btn btn-sm btn-outline-secondary" @onclick="async () => await OnQuickBet(0.50m)" disabled="@(ViewModel.Balance <= 0 || ViewModel.TimeLeft < 5 || ViewModel.IsLoading)">
                                50%
                            </button>
                            <button type="button" class="btn btn-sm btn-outline-secondary" @onclick="async () => await OnQuickBet(0.75m)" disabled="@(ViewModel.Balance <= 0 || ViewModel.TimeLeft < 5 || ViewModel.IsLoading)">
                                75%
                            </button>
                            <button type="button" class="btn btn-sm btn-outline-secondary" @onclick="async () => await OnQuickBet(1.00m)" disabled="@(ViewModel.Balance <= 0 || ViewModel.TimeLeft < 5 || ViewModel.IsLoading)">
                                100%
                            </button>
                        </div>
                    </div>
                    <div class="bet-buttons">
                        <button class="btn btn-odd @(ViewModel.SelectedChoice == "홀" ? "selected" : "")"
                                @onclick="ViewModel.SelectOdd"
                                disabled="@(ViewModel.TimeLeft < 5 || ViewModel.IsLoading || ViewModel.IsPlacingBet)">
                            홀 (x2.0)
                        </button>
                        <button class="btn btn-even @(ViewModel.SelectedChoice == "짝" ? "selected" : "")"
                                @onclick="ViewModel.SelectEven"
                                disabled="@(ViewModel.TimeLeft < 5 || ViewModel.IsLoading || ViewModel.IsPlacingBet)">
                            짝 (x2.0)
                        </button>
                    </div>
                    @if (!string.IsNullOrEmpty(ViewModel.SelectedChoice) && ViewModel.BetAmount > 0)
                    {
                        <div class="bet-summary">
                            <p>선택: <strong>@ViewModel.SelectedChoice</strong></p>
                            <p>베팅 금액: <strong>@ViewModel.BetAmount.ToString("N0") 원</strong></p>
                            <p>예상 당첨금: <strong>@((ViewModel.BetAmount * 2).ToString("N0")) 원</strong></p>
                        </div>
                    }
                    <button class="btn btn-primary btn-bet"
                            @onclick="PlaceBet"
                            disabled="@ViewModel.IsBetDisabled">
                        @if (ViewModel.IsPlacingBet)
                        {
                            <span class="spinner-border spinner-border-sm me-2"></span>
                        }
                        베팅하기
                    </button>
                </div>
            </div>

            <div class="game-history">
                <h3>베팅 내역</h3>
                @if (ViewModel.Bets == null || ViewModel.Bets.Count == 0)
                {
                    <div class="empty-history">
                        <p>베팅 내역이 없습니다.</p>
                    </div>
                }
                else
                {
                    <div class="bets-list">
                        @foreach (var bet in ViewModel.Bets)
                        {
                            <div class="bet-item @bet.Status.ToString().ToLower()">
                                <div class="bet-info">
                                    <span class="bet-choice">@bet.BetChoice</span>
                                    <span class="bet-amount">@bet.Amount.ToString("N0") 원</span>
                                </div>
                                <div class="bet-result">
                                    <span class="result">결과: @bet.Result</span>
                                    @if (bet.Status == GameBetStatus.Won)
                                    {
                                        <span class="win-amount">+@bet.WinAmount.ToString("N0") 원</span>
                                    }
                                    else if (bet.Status == GameBetStatus.Lost)
                                    {
                                        <span class="lose-amount">-@bet.Amount.ToString("N0") 원</span>
                                    }
                                    else
                                    {
                                        <span class="pending">대기중...</span>
                                    }
                                </div>
                                <div class="bet-time">@bet.BetTime.ToString("HH:mm:ss")</div>
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>
}

@code {
    private void NavigateToLogin() => Navigation.NavigateTo("/login");

    protected override async Task OnInitializedAsync()
    {
        ViewModel.StateChanged += OnViewModelStateChanged;
        await ViewModel.InitializeAsync();
    }

    private Task OnQuickBet(decimal percentage) => ViewModel.SetBetAmountPercentageAsync(percentage);
    private Task OnBetAmountInput(ChangeEventArgs e) => ViewModel.UpdateBetAmountFromInputAsync(e.Value?.ToString());
    private Task PlaceBet() => ViewModel.PlaceBetAsync();

    private void OnViewModelStateChanged() => InvokeAsync(StateHasChanged);

    public void Dispose()
    {
        ViewModel.StateChanged -= OnViewModelStateChanged;
        ViewModel.Dispose();
    }
}

